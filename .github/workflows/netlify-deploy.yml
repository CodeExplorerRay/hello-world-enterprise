name: Frontend Deployment Notice

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest

    steps:
    - name: Explain frontend deployment path
      run: |
        echo "Frontend deployment has moved to Vercel."
        echo "Netlify is no longer part of the production deployment path for this repo."
        echo "See docs/vercel-frontend-deployment.md for setup."
